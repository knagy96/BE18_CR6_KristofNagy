<form method="post" action="{{ path('app_c_r_delete', {'id': c_r.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_r.id) }}">
    <button class="btn btn-danger my-3">Delete</button>
</form>